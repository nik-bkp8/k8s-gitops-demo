apiVersion: v1
kind: Service
metadata:
  name: polling-app
  namespace: polling-app-prod
spec:
  selector:
    app: polling-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
