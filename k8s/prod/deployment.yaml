apiVersion: apps/v1
kind: Deployment
metadata:
  name: polling-app
  namespace: polling-app-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: polling-app
  template:
    metadata:
      labels:
        app: polling-app
    spec:
      containers:
      - name: polling-app
        image: nginx:latest   # same placeholder image
        ports:
        - containerPort: 80
