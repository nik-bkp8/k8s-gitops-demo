apiVersion: apps/v1
kind: Deployment
metadata:
  name: polling-app
  namespace: polling-app-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: polling-app
  template:
    metadata:
      labels:
        app: polling-app
    spec:
      containers:
      - name: polling-app
        image: your-registry/polling-app:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
